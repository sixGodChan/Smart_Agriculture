version: '3.8'

services:
  crop-guard-web:
    build: .
    container_name: crop-guard-web
    ports:
      - "5173:5173"
    environment:
      - API_KEY=${API_KEY}
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped